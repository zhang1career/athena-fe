APP_NAME=athena-fe

# next dev / next start 监听端口
PORT=13901

# API 配置（二选一）：
# 1）服务器部署（推荐）：留空或注释，前端走同源 /api，由 Next 代理到后端，无需 CORS
# 2）本地直连：填后端地址（勿带尾部斜杠），需后端允许 CORS
# NEXT_PUBLIC_API_BASE_URL=

# 代理目标：当 NEXT_PUBLIC_API_BASE_URL 为空时，rewrites 将 /api/v1/* 代理到此地址
API_BACKEND_URL=http://127.0.0.1:19001
